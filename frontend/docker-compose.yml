version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Override to point at a different backend
        API_BASE_URL: http://35.244.19.78:8000
    ports:
      - "80:80"
    restart: unless-stopped
    security_opt:
      - seccomp:unconfined
